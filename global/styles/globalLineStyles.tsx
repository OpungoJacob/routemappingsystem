import { LineLayerStyle } from "@rnmapbox/maps";

export const lineStyle: LineLayerStyle = {
  lineLayer: {
    lineColor: "red",
    lineJoin: "round",
    lineCap: "round",
    lineWidth: 3,
  },
};
